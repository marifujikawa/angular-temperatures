import { MaskFloatDirective } from './mask-float.directive';

describe('MaskFloatDirective', () => {
  it('should create an instance', () => {
    const directive = new MaskFloatDirective();
    expect(directive).toBeTruthy();
  });
});
